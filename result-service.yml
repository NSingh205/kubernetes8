apiVersion: v1
kind: Service
metadata:
  name: result
  labels:
    app: result
spec:
  type: NodePort
  ports:
   - port: 85
     targetPort: 85
     nodePort: 30010 
  selector:
    app: result             









