apiVersion: v1
kind: Service
metadata:
  name: result-service
  labels:
    app: result-service
spec:
  type: NodePort
  ports:
   - port: 81
     targetPort: 80
     nodePort: 30010 
  selector:
    app: result             









