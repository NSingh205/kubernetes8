apiVersion: v1
kind: Service
metadata:
  name: redis-service
  labels:
    app: redis-service
spec:
  type: ClusterIP
  ports:
   - port: 6380
     targetPort: 6379 
  selector:
    app: redis              
